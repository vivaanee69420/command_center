[build]
builder = "railpack"
buildCommand = "pip install -r COMMAND_OS_BACKEND/requirements.txt"

[deploy]
startCommand = "cd COMMAND_OS_BACKEND && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
